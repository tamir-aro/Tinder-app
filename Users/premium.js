import profile from "./profile";

export default class premium extends profile {
    constructor(hobbies) {
        
        super(id, name, gender, age, height, location, image, premium)
        this.hobbies = hobbies;

    }

      render() {
        super(temp);
        hobbies.map(temp => temp += `<p>${hobbies} </p>`);
        
    }
    
}